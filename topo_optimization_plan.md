# 电池包底板拓扑优化分析说明（独立底板）

## 1. 建模边界
- 独立对象：仅底板本体（不含电芯、上盖、侧梁）。
- 几何初版：`1600 x 1200 x 4 mm` 平板，含安装孔。
- 设计域：除安装孔、密封面、边框装配带外，其余区域允许材料删除。

## 2. 材料与约束
- 材料：铝合金（示例：AA6061-T6），`E=69 GPa`，`ν=0.33`，`ρ=2700 kg/m³`。
- 约束：边框安装点按车身连接刚度进行等效弹簧约束；孔周区域设为非设计区。

## 3. 多工况受力定义

### 工况 A：整包静载 + 动载放大
- 载荷：电池模组重力等效面载 + `2.5g` 垂向惯性。
- 目标：限制最大挠度，降低跨中应力集中。

### 工况 B：底部托底冲击（准静态等效）
- 载荷：底板局部 `200 mm x 200 mm` 区域集中向上载荷。
- 目标：形成局部力流通路，提升抗凹陷能力。

### 工况 C：纵向冲击惯性
- 载荷：沿车长方向 `15g` 惯性载荷。
- 目标：提升纵向与对角向整体刚度，控制安装孔附近塑性风险。

### 工况 D：扭转载荷
- 载荷：四角反向位移/力耦合输入，模拟车身扭转传递。
- 目标：提高扭转刚度并维持边框密封面平面度。

## 4. 拓扑优化设置（建议）
- 优化方法：SIMP 密度法。
- 目标函数：最小柔度（最大刚度）。
- 体积分数约束：`35% ~ 45%`（按工艺逐步迭代）。
- 最小特征尺寸：`>= 12 mm`（满足压铸/机加可制造性）。
- 对称约束：按中心面对称（若布置允许）。
- 多工况权重：A:B:C:D = `0.35:0.25:0.25:0.15`。

## 5. 结果解读与重建原则
- 高频保留区域：边框封闭环、安装孔邻域、跨中对角力流区。
- 高频删减区域：低应变能密度的大片平板区。
- CAD 重建：以“边框承载带 + 纵横筋 + 对角筋 + 局部实体岛”替代有机云图。
- 已在 `battery_baseplate.scad` 中按上述原则重建拓扑优化后底板结构。

## 6. 交付建议
- 导出原始底板与优化底板 STEP/GLB 进行评审。
- 在详细设计阶段补充：模态、疲劳、热-结构耦合与跌落冲击显式分析。
